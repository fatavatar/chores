version: '2'

services:
  chores_client:
    image: chores_client
    restart: always
    ports:
      - 8002:80

  chores_api:
    image: chores_api
    restart: always
    ports:
      - 8001:8080

